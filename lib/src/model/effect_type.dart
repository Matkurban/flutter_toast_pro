/// Effect type for message colors.
///
/// message 颜色效果类型。
enum EffectType {
  /// Primary light tone.
  ///
  /// 浅色调（primary light）。
  primaryLight,

  /// Primary tone.
  ///
  /// 主色调（primary）。
  primary,
}
